useDynLib(pbdBASE)

import(methods, pbdMPI, pbdSLAP)

# All internal functions
exportPattern("^base\\.[[:alpha:]]+")

export(
  ### blacs
  blacs_gridinit, 
  init.grid, 
  blacs, 
  gridexit, 
  pnum, 
  pcoord, 
  blacsexit, 
  minctxt,
  ### utility
  numroc
)

export(
  .Last.lib    # For finalize blacs.
)
